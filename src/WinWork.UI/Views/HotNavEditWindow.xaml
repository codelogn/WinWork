<Window x:Class="WinWork.UI.Views.HotNavEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Hot Nav" Height="420" Width="520">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Name:" VerticalAlignment="Center" Width="60"/>
            <TextBox x:Name="NameTextBox" Width="380" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,8">
            <CheckBox x:Name="IncludeFilesCheckBox" Content="Include files in menus"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,0,0,8">
            <TextBlock Text="Max Depth (empty = 16):" VerticalAlignment="Center" Width="160"/>
            <TextBox x:Name="MaxDepthTextBox" Width="80" />
        </StackPanel>

        <GroupBox Header="Roots" Grid.Row="3">
            <Grid Margin="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid x:Name="RootsDataGrid" Grid.Row="0" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="False" RowHeaderWidth="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Path" Binding="{Binding Path, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Width="*" />
                        <DataGridTextColumn Header="Order" Binding="{Binding SortOrder}" Width="80" IsReadOnly="True" />
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button Content="Add Root" Width="90" Click="AddRoot_Click" Margin="4"/>
                    <Button Content="Edit (Inline)" Width="110" Click="EditRoot_Click" Margin="4"/>
                    <Button Content="Remove Root" Width="90" Click="RemoveRoot_Click" Margin="4"/>
                    <Button Content="Move Up" Width="80" Click="MoveUp_Click" Margin="4"/>
                    <Button Content="Move Down" Width="80" Click="MoveDown_Click" Margin="4"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <StackPanel Orientation="Horizontal" Grid.Row="4" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Save" Width="90" Click="Save_Click" Margin="4"/>
            <Button Content="Cancel" Width="90" Click="Cancel_Click" Margin="4"/>
        </StackPanel>
    </Grid>
</Window>
